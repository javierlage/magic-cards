# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Magic Cards' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Magic Cards
  
  # API for rest
  pod 'Alamofire', '~> 5.2'
  # Handle remotes images
  pod 'Kingfisher', '~> 5.0'

  target 'Magic CardsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Magic CardsUITests' do
    # Pods for testing
  end

end
